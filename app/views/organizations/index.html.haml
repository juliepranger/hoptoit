%h1 Organizations#Index

=notice
=link_to "new org", new_organization_path

%ul
  -@orgs.each do |o|
    %li
      ="#{o.organization_name}, #{o.city}, #{o.state}"
      =link_to "view", organization_path(o.id)