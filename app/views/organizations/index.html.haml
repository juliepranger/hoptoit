%h1 Organizations

=link_to "new org", organizations_search_path

%ul
  -@orgs.each do |o|
    %li
      ="#{o.organization_name}, #{o.city}, #{o.state}"
      =link_to "view", organization_path(o.id)